{
  "name": "trophoria",
  "scripts": {
    "start": "bun run --watch src/index.ts",
    "lint": "tsc & prettier --ignore-path ../.gitignore --write --check . & eslint ."
  },
  "dependencies": {
    "elysia": "^0.6.3"
  },
  "devDependencies": {
    "@modules/eslint-config": "*",
    "@modules/prettier-config": "*",
    "bun-types": "latest"
  },
  "prettier": "@modules/prettier-config",
  "eslintConfig": {
    "extends": [
      "@modules/eslint-config"
    ]
  }
}
